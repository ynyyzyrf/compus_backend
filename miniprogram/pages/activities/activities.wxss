.head {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.subtitle {
  color: var(--muted);
  font-size: 25rpx;
  margin-top: -8rpx;
}
.head-icon {
  width: 78rpx;
  height: 78rpx;
  border-radius: 24rpx;
  background: var(--brand-soft);
  color: var(--brand);
  display: flex;
  align-items: center;
  justify-content: center;
}
.pill-scroll {
  height: 86rpx;
  margin: 20rpx 0 24rpx;
  white-space: nowrap;
}
.pill-scroll .pill-tabs {
  display: inline-flex;
  margin: 0;
  min-width: 100%;
}
.pill-scroll .pill-tab {
  flex: 0 0 auto;
}
.activity-list {
  display: flex;
  flex-direction: column;
  gap: 30rpx;
}
.activity-card {
  overflow: hidden;
  background: #fff;
  border: 1rpx solid var(--line);
  border-radius: 26rpx;
  box-shadow: 0 18rpx 42rpx rgba(126, 40, 34, 0.12);
}
.image-wrap {
  position: relative;
  height: 360rpx;
}
.cover {
  width: 100%;
  height: 100%;
}
.date-badge {
  position: absolute;
  top: 18rpx;
  left: 18rpx;
  width: 82rpx;
  height: 92rpx;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, 0.94);
  color: var(--ink);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 22rpx rgba(17, 24, 39, 0.12);
}
.month {
  font-size: 22rpx;
  color: var(--muted);
}
.day {
  font-size: 34rpx;
  line-height: 1.05;
  font-weight: 900;
}
.body {
  padding: 24rpx 26rpx 26rpx;
}
.title {
  font-size: 32rpx;
  line-height: 1.38;
  font-weight: 900;
  color: var(--ink);
  margin-bottom: 16rpx;
}
.row {
  display: flex;
  align-items: center;
  gap: 10rpx;
  color: var(--muted);
  font-size: 27rpx;
  line-height: 1.7;
}
.footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 22rpx;
}
.signup-summary {
  flex: 1;
  min-width: 0;
  color: var(--muted);
  font-size: 27rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex-wrap: wrap;
}
.remaining {
  color: #c57a24;
  font-weight: 800;
}
.status {
  height: 64rpx;
  min-width: 176rpx;
  padding: 0 30rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 700;
}
.status.primary {
  color: #fff;
  background: linear-gradient(135deg, var(--brand), var(--brand-2));
}
.status.soft {
  color: var(--brand);
  background: var(--brand-soft);
}
.status.warm {
  color: #bb6400;
  background: #fff4df;
}
.status.muted {
  color: var(--muted);
  background: #eef3fb;
}
